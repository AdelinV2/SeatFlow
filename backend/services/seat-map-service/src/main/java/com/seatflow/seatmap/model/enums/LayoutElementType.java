package com.seatflow.seatmap.model.enums;

/**
 * Non-bookable visual primitives stored in {@code venue_layout_elements}.
 * Mirrors V5 {@code chk_venue_layout_elements_type} exactly.
 */
public enum LayoutElementType {
    STAGE,
    AISLE,
    LABEL,
    BARRIER,
    DECORATION
}
